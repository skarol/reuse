import Foundation
import Quick
import Nimble
@testable import ProjectName

class ClassNameTests: QuickSpec {

    override func spec() {

        var className: ClassName!

        describe("ClassName") {
            beforeEach {
                className = ClassName()
                
            }

            context("when ") {
            
                it("") {
                }
            }    
        }
    }
}